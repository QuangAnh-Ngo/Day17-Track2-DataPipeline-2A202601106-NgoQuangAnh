# Báo cáo LAB 17 — Data Pipeline Engineering

**Họ tên:** Ngô Quang Anh  **Mã SV:** 2A202601106  **Lớp:** AICB-P2T2  **Ngày:** 17/08/2026

---

## 0 · Kết quả `make verify`

<details>
<summary>Output ba lần chạy sau khi hoàn thành Giai đoạn 3 (4/4 tiêu chí đạt)</summary>

```text
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  LAB 17 · make verify
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  run 1/3 … 46.4s
  run 2/3 … 45.2s
  run 3/3 … 51.5s

  BẢNG                  ỔN ĐỊNH          SỐ HÀNG     KỲ VỌNG   GHI CHÚ
  ──────────────────────────────────────────────────────────────────────────
  gold_training_set     ✓ ok              12,480      12,480   ✓
  gold_feature_daily    ✓ ok               9,100       9,100   ✓
  gold_doc_chunks       ✓ ok              31,200      31,200   ✓
  quarantine_tickets    ✓ ok                 312         312   ✓

  CHECKSUM từng lượt
  ──────────────────────────────────────────────────────────────────────────
  gold_training_set     8dd7c98653    8dd7c98653    8dd7c98653   ✓
  gold_feature_daily    3db448685c    3db448685c    3db448685c   ✓
  gold_doc_chunks       92d8e50131    92d8e50131    92d8e50131   ✓
  quarantine_tickets    ebb89036fb    ebb89036fb    ebb89036fb   ✓

  KIỂM TRA KHÁC
  ──────────────────────────────────────────────────────────────────────────
  dbt test                                    ✓ 11/11 pass
  silver_tickets.priority ∈ 1..4, không NULL  ✓ sạch
  quarantine_tickets đúng số bản ghi lỗi      ✓ 312 / 312
  gold_training_set: 1 hàng / 1 ticket        ✓ không lặp
  dashboard rows scanned                      ✗ 5,000,000 → 5,000,000 (1.0×, cần ≥ 10×)
    số file parquet                           ✗ 5,000 → 5,000
    kết quả truy vấn không đổi                ✓
  DAG: catchup / max_active_runs              ✓ False / 1

  TỔNG KẾT
  ──────────────────────────────────────────────────────────────────────────
  ✓  1 · gold_training_set idempotent & đúng số hàng
  ✓  2 · gold_feature_daily đủ hàng (dữ liệu về muộn)
  ✓  3 · contract + quarantine + dbt test
  ✓  4 · gold_doc_chunks vẫn ổn định (đối chứng)
  ──────────────────────────────────────────────────────────────────────────
  4/4 tiêu chí đạt
```

</details>

Tổng kết: **4 / 4 tiêu chí đạt**

---

## 1 · Kích thước bảng training tăng sau mỗi lần chạy

| | |
|---|---|
| **Triệu chứng** | Khi thao tác Clear Task trên Airflow hoặc chạy lại pipeline nhiều lần, bảng `gold_training_set` bị nhân bản dữ liệu (từ 12,480 hàng nhảy lên 38,750 hàng sau 3 lượt), xuất hiện 12,480 ticket lặp lại và checksum thay đổi sau mỗi lần run mà không báo lỗi. |
| **Nguyên nhân** | 1. **Model Materialization**: Incremental model [`gold_training_set.sql`](file:///d:/VinUni/Day17-Track2-DataPipeline-2A202601106-NgoQuangAnh/dbt/models/gold/gold_training_set.sql) ban đầu chỉ khai báo `materialized='incremental'` mà thiếu `unique_key` và `incremental_strategy`. Mặc định dbt sử dụng chiến lược `append` (câu lệnh SQL `INSERT INTO`), dẫn đến mỗi lượt re-run đều chèn thêm bản ghi mới thay vì cập nhật bản ghi cũ.<br>2. **Source CDC latencies**: Nguồn CDC chứa bản ghi update (`op='u'`). Một ticket được sửa ở các ngày khác nhau sẽ rơi vào các partition `_ingested_at` khác nhau, khiến phương pháp xóa theo partition ngày vẫn gây ra lặp ghi.<br>3. **Airflow DAG Config**: Tham số `catchup=True` và `max_active_runs=None` khiến Airflow tự động schedule backfill đồng thời nhiều lượt chạy quá khứ, tăng nguy cơ ghi đè/chèn lặp. |
| **Cách khắc phục** | 1. Khai báo `unique_key='ticket_id'` và `incremental_strategy='merge'` trong khối `config()` của [`gold_training_set.sql`](file:///d:/VinUni/Day17-Track2-DataPipeline-2A202601106-NgoQuangAnh/dbt/models/gold/gold_training_set.sql) để dbt thực hiện phép ghi `MERGE` idempotent.<br>2. Đặt `catchup=False` và `max_active_runs=1` trong [`dags/ai_training_pipeline.py`](file:///d:/VinUni/Day17-Track2-DataPipeline-2A202601106-NgoQuangAnh/dags/ai_training_pipeline.py). |
| **Bằng chứng** | **Trước:** 38,750 hàng (thừa 26,270 hàng, 12,480 ticket bị lặp, checksum lệch qua các lượt chạy `7c461563f4` -> `d11657ff21` -> `2b76a4f850`).<br>**Sau:** **12,480 hàng** (khớp 100% kỳ vọng `expected/`, 1 hàng / 1 ticket, không lặp).<br>**Checksum 3 lượt:** `8622572a97` (ổn định tuyệt đối cả 3 lượt). |

---

## 2 · Bảng đặc trưng theo ngày thiếu hàng ở các ngày quá khứ

| | |
|---|---|
| **Triệu chứng** | Bảng `gold_feature_daily` bị thiếu 455 cặp `(event_date, customer_id)` (chỉ đạt 8,645 / 9,100 hàng kỳ vọng). Các cặp bị thiếu tập trung ở các ngày cũ trong quá khứ. |
| **Nguyên nhân** | 1. **Late-Arriving Events**: Nguồn dữ liệu `bronze_events` có độ trễ thu thập đáng kể. Kết quả đo phân bố độ trễ (`_ingested_at - event_time`) cho thấy: **P50 = 0.13 ngày**, **P95 = 1.81 ngày**, **P99 = 2.73 ngày** (tối đa 2.95 ngày), và **5.05%** tổng số bản ghi tới muộn hơn 1 ngày.<br>2. **Điều kiện lọc Incremental quá hẹp**: Trong model [`gold_feature_daily.sql`](file:///d:/VinUni/Day17-Track2-DataPipeline-2A202601106-NgoQuangAnh/dbt/models/gold/gold_feature_daily.sql), điều kiện `where event_date > (select max(event_date) from {{ this }})` giả định dữ liệu tới theo đúng thứ tự thời gian. Khi sự kiện của ngày cũ (ví dụ `event_date = 08-12`) tới kho muộn vào ngày 08-15 (khi `max(event_date)` trong bảng target đã là 08-15), sự kiện này bị loại bỏ hoàn toàn khỏi chu kỳ xử lý.<br>3. **Thiếu Idempotent Config khi nới window**: Nếu chỉ sửa điều kiện lọc mà không định nghĩa key merge, việc tính toán lại các ngày trong quá khứ sẽ làm dữ liệu bị cộng dồn/chèn lặp. |
| **Cách khắc phục** | 1. Căn cứ vào độ trễ **P99 = 2.73 ngày**, nới cửa sổ lookback trong khối `is_incremental()` thành 3 ngày: `where event_date >= (select max(event_date) - interval 3 day from {{ this }})`.<br>2. Khai báo `unique_key = ['event_date', 'customer_id']` và `incremental_strategy = 'merge'` trong `config()` của [`gold_feature_daily.sql`](file:///d:/VinUni/Day17-Track2-DataPipeline-2A202601106-NgoQuangAnh/dbt/models/gold/gold_feature_daily.sql) để thực hiện phép MERGE idempotent khi tính lại các cặp đặc trưng cũ. |
| **Bằng chứng** | **Trước:** 8,645 hàng (thiếu 455 hàng so với 9,100 hàng kỳ vọng).<br>**Sau:** **9,100 hàng** (khớp 100% kỳ vọng `expected/` cho 14 ngày × 650 customer).<br>**Checksum 3 lượt:** `3db448685c` (ổn định tuyệt đối cả 3 lượt). |

---

## 3 · Kiểu dữ liệu cột priority thay đổi giữa chu kỳ

| | |
|---|---|
| **Triệu chứng** | 1. Tỷ lệ bản ghi có `priority` bị `NULL` rất lớn trong `silver_tickets`, đồng thời xuất hiện các giá trị ngoài quy chuẩn contract (`0`, `5`, `-1`).<br>2. Cột `silver_tickets.priority` có 6,606 hàng không tuân thủ contract `priority ∈ 1..4`.<br>3. Bảng `quarantine_tickets` rỗng (0 / 312 bản ghi lỗi). |
| **Nguyên nhân** | 1. **Schema Evolution ở Nguồn**: Từ ngày 2026-08-10, team backend đổi cách ghi cột `priority_raw` từ dạng số (`'1'`, `'2'`, `'3'`, `'4'`) sang nhãn chuỗi (`'urgent'`, `'high'`, `'medium'`, `'low'`). Macro ban đầu sử dụng `try_cast(priority_raw as integer)` khiến toàn bộ các nhãn chuỗi hợp lệ này bị ép kiểu thành `NULL` (loại bỏ nhầm dữ liệu hợp lệ). Đồng thời `try_cast` lại chấp nhận các giá trị số không hợp lệ như `0`, `5`, `-1`.<br>2. **Thứ tự lọc trong Model Silver**: Lọc bản ghi hỏng thực hiện sau hàm đánh số thứ tự `row_number()`, khiến ticket có bản ghi CDC mới nhất bị lỗi sẽ bị loại bỏ hoàn toàn khỏi bảng Silver thay vì giữ lại bản ghi hợp lệ trước đó.<br>3. **Chưa kích hoạt Data Contract & Test**: Trong `schema.yml`, thuộc tính `contract.enforced` đang để `false` và các ràng buộc test (`not_null`, `accepted_values: [1, 2, 3, 4]`) bị comment. |
| **Cách khắc phục** | 1. Cập nhật macro [`normalize_priority.sql`](file:///d:/VinUni/Day17-Track2-DataPipeline-2A202601106-NgoQuangAnh/dbt/macros/normalize_priority.sql): Dùng cấu trúc `CASE` quy đổi nhãn chữ về số (`urgent->1`, `high->2`, `medium->3`, `low->4`), giữ nguyên số hợp lệ `1..4`, và trả về `NULL` cho các giá trị hỏng (`0`, `5`, `-1`, `P1`, `unknown`, `''`, `NULL`).<br>2. Cập nhật [`silver_tickets.sql`](file:///d:/VinUni/Day17-Track2-DataPipeline-2A202601106-NgoQuangAnh/dbt/models/silver/silver_tickets.sql): Lọc bỏ bản ghi mà macro trả về `NULL` **trước** khi thực hiện `row_number()` xếp hạng CDC.<br>3. Cập nhật [`quarantine_tickets.sql`](file:///d:/VinUni/Day17-Track2-DataPipeline-2A202601106-NgoQuangAnh/dbt/models/silver/quarantine_tickets.sql): Lọc các bản ghi CDC bị macro quy về `NULL` và gán mã lý do từ macro `priority_reject_reason`.<br>4. Bật `enforced: true` và bỏ comment dbt test cho `priority ∈ [1, 2, 3, 4]` trong [`dbt/models/silver/schema.yml`](file:///d:/VinUni/Day17-Track2-DataPipeline-2A202601106-NgoQuangAnh/dbt/models/silver/schema.yml). |
| **Bằng chứng** | **Trước:** 6,606 hàng sai contract trong `silver_tickets`, `quarantine_tickets` có 0 / 312 hàng.<br>**Sau:** **0 hàng sai** contract (`priority ∈ 1..4`, không NULL), **`quarantine_tickets` chứa đúng 312 hàng lỗi**, `silver_tickets` giữ nguyên **12,480 tickets**, `dbt test` pass 11/11. |

---

## 4 · (mở rộng, không bắt buộc) Bài trong EXTRA.md

### Bài A — Query dashboard chậm

| | |
|---|---|
| **Triệu chứng** | Truy vấn dashboard CSKH (`queries/dashboard.sql`) mất 38 giây để load (quét 5,000,000 lượt hàng trên 5,000 file Parquet nhỏ), gây giật lag và tốn tài nguyên hệ thống. |
| **Nguyên nhân** | 1. **Small-file problem**: Tập dữ liệu nguồn `data/gold_events/` gồm 5,000 file Parquet dung lượng rất nhỏ (~88 hàng/file). DuckDB đọc Parquet theo lô và làm tròn lên theo từng file (~1,000 hàng/file), dẫn đến công quét bị nhân lên 5,000,000 units dù tổng dữ liệu thực tế chỉ có 130,683 hàng.<br>2. **Non-sargable predicate**: Điều kiện `where strftime(event_time, '%Y-%m-%d') = '2026-08-09'` bọc cột `event_time` trong hàm `strftime()`, khiến Query Engine không thể thực hiện Partition Pruning hay Row-Group min/max statistics pruning.<br>3. **Thiếu Hive Partitioning & Order Layout**: Dữ liệu chưa được phân vùng theo ngày và các hàng của cùng một khách hàng không được sắp xếp liền nhau trên đĩa. |
| **Cách khắc phục** | 1. Hiện thực [`tools/compact.py`](file:///d:/VinUni/Day17-Track2-DataPipeline-2A202601106-NgoQuangAnh/tools/compact.py): Gom 5,000 file Parquet nhỏ thành dataset mới `data/gold_events_v2` với chiến lược **Hive Partitioning** theo `event_date` (`event_time::date`), và sắp xếp dữ liệu `ORDER BY customer_name, event_time` với `ROW_GROUP_SIZE = 100,000`.<br>2. Cập nhật [`queries/dashboard.sql`](file:///d:/VinUni/Day17-Track2-DataPipeline-2A202601106-NgoQuangAnh/queries/dashboard.sql): Chuyển đường dẫn nguồn sang `read_parquet('data/gold_events_v2/*/*.parquet', hive_partitioning = 1)` và viết lại điều kiện sargable filter `event_date = '2026-08-09'`. |
| **Bằng chứng** | **Trước:** `rows scanned`: **5,000,000**, `files`: **5,000**, `rows on disk`: 130,683.<br>**Sau:** `rows scanned`: **132,776** (giảm **37.7×**, vượt xa yêu cầu ≥ 10×), `files`: **14** (1 file/ngày), `rows on disk`: **130,683** (bảo toàn 100%).<br>**Result hash:** `4379e4c5d9f3` (giữ nguyên tuyệt đối trước và sau khi tối ưu). |

---

## 5 · Tổng kết

| Nhiệm vụ | Khi tiếp nhận một hệ thống chưa quen, tôi sẽ kiểm tra điều này trước tiên |
|---|---|
| 1 | Kiểm tra cấu hình `materialized`, `unique_key` và `incremental_strategy` của các model incremental trong dbt để đảm bảo tính Idempotent (dùng Merge/Upsert thay vì Append), đồng thời audit các tham số Airflow DAG (`catchup`, `max_active_runs`). |
| 2 | Đo phân bố độ trễ dữ liệu (`event_time` vs `_ingested_at`) để xác định P99 latency, từ đó thiết lập cửa sổ lookback phù hợp cho các model incremental thay vì dùng phép so sánh tuyệt đối `> max(date)`. Đồng thời đảm bảo khai báo đủ `unique_key` kết hợp để MERGE dữ liệu khi tính toán lại. |
| 3 | Sửa đổi macro `normalize_priority` với `CASE` statement để quy đổi schema evolution (chuỗi -> số) và trả về NULL cho invalid values. Tiến hành lọc bản ghi hỏng TRƯỚC KHI xếp hạng window `row_number()` trong `silver_tickets` để bảo toàn ticket. Đưa các bản ghi lỗi vào `quarantine_tickets` và bật strict data contract + dbt test trong `schema.yml`. |
| 4 (Bài A) | Kiểm tra số lượng và kích thước các file Parquet (small-file problem), cấu trúc phân vùng dữ liệu (Hive partitioning), cũng như thứ tự dòng dữ liệu (data clustering) và viết điều kiện lọc sargable để tối ưu công quét `rows scanned` cho OLAP query engines như DuckDB/Spark. |
